<?xml version="1.0"  encoding="ISO-8859-1"?>
<!DOCTYPE Configure PUBLIC "-//Mort Bay Consulting//DTD Configure//EN" "http://jetty.mortbay.org/configure.dtd">
<Configure id="crudzilla-websiteApp" class="org.eclipse.jetty.webapp.WebAppContext">  
  
  <Set name="contextPath">/crudzilla-website</Set>
  <Set name="war"><SystemProperty name="jetty.home" default="."/>/webapps/crudzilla-website.war</Set>
  
  
  
  <Get name="securityHandler">
    <Set name="loginService">
     <New class="org.eclipse.jetty.security.JDBCLoginService">
	    <Set name="name">crudzilla-website</Set>
	    <Set name="config"><SystemProperty name="jetty.home" default="."/>/etc/crudzilla-website.jdbc.realm.properties</Set>
            <!-- To enable reload of realm when properties change, uncomment the following lines -->
            <!-- changing refreshInterval (in seconds) as desired                                -->
            <!--
            <Set name="refreshInterval">5</Set>
            <Call name="start"></Call>
            -->
      </New>
    </Set>
    <Set name="checkWelcomeFiles">true</Set>
  </Get>

</Configure>
